{"id":1062940436,"lang":"cpp","lang_name":"C++","time":"1 month, 2 weeks","timestamp":1696068023,"status":10,"status_display":"Accepted","runtime":"11 ms","url":"/submissions/detail/1062940436/","is_pending":"Not Pending","title":"Asteroid Collision","memory":"18.2 MB","code":"class Solution {\npublic:\n    vector<int> asteroidCollision(vector<int>& a) {\n        // vector<int> ans;\n        // stack<int> st;\n        // for (int i = 0; i < a.size(); i++) {\n        //     if (st.empty() || a[i] > 0) {\n        //         st.push(a[i]);\n        //     } else {\n        //         while (!st.empty() && st.top() > 0 && st.top() < abs(a[i])) {\n        //             st.pop();\n        //         }\n        //         if (st.empty() || st.top() < 0) {\n        //             st.push(a[i]);\n        //         } else if (st.top() == abs(a[i])) {\n        //             st.pop();\n        //         }\n        //     }\n        // }\n        // while (!st.empty()) {\n        //     ans.push_back(st.top());\n        //     st.pop();\n        // }\n        // reverse(ans.begin(), ans.end());\n        // return ans;\n        \n        vector<int> ans;\n        stack<int> st;\n//         for(int i=0;i<a.size();i++){\n//             if(st.empty()||a[i]>0)st.push(a[i]);\n//             else{\n//              while(!st.empty()&&st.top()>0&&st.top()<abs(a[i]))st.pop();\n//                 if(st.empty()||st.top()<0)st.push(a[i]);\n//                 else if(st.top()==abs(a[i]))st.pop();\n                \n//             }\n//         }\n           \n        for(int i=0;i<a.size();i++){\n            if(st.empty()||a[i]>0)st.push(a[i]);\n            else{\n                while(!st.empty()&&st.top()>0&&st.top()<abs(a[i]))st.pop();\n                if(st.empty()||st.top()<0)st.push(a[i]);\n                else if(st.top()==abs(a[i])){\n                    st.pop();\n                }\n            }\n        }\n        \n        \n        \n         while (!st.empty()) {\n            ans.push_back(st.top());\n            st.pop();\n        }\n        reverse(ans.begin(), ans.end());\n        return ans;\n    }\n};\n","compare_result":"","title_slug":"asteroid-collision","has_notes":false,"flag_type":1}